services:
    postgres-database:
        image: postgres:18.3-alpine3.23
        container_name: readme-postgres-database
        command:
            - "postgres"
            - "-c"
            - "max_connections=50"
            - "-c"
            - "shared_buffers=1GB"
            - "-c"
            - "effective_cache_size=4GB"
            - "-c"
            - "work_mem=16MB"
            - "-c"
            - "maintenance_work_mem=512MB"
            - "-c"
            - "random_page_cost=1.1"
            - "-c"
            - "temp_file_limit=5GB"
            - "-c"
            - "log_min_duration_statement=200ms"
            - "-c"
            - "idle_in_transaction_session_timeout=10s"
            - "-c"
            - "lock_timeout=1s"
            - "-c"
            - "statement_timeout=60s"
            - "-c"
            - "shared_preload_libraries=pg_stat_statements"
            - "-c"
            - "pg_stat_statements.max=10000"
            - "-c"
            - "pg_stat_statements.track=all"
        environment:
            POSTGRES_DB: "readmedb"
            POSTGRES_USER: "readmepguser"
            POSTGRES_PASSWORD: "pgpwd4readme"
            PGDATA: "/var/lib/postgresql/data/pgdata"
        volumes:
            - readme-postgres-database-data:/var/lib/postgresql/data
        ports:
            - "5432:5432"
        networks:
            - readme-network
        healthcheck:
            test: [ "CMD-SHELL", "pg_isready -U readmepguser -d readmedb" ]
            interval: 10s
            timeout: 5s
            retries: 5
            start_period: 10s
        restart: unless-stopped
        deploy:
            resources:
                limits:
                    cpus: '1'
                    memory: 1G

    redis-database:
        image: redis:7.4.8-alpine3.21
        container_name: readme-redis-database
        command: ["redis-server", "/usr/local/etc/redis/redis.conf", "--requirepass", "${REDIS_PASSWORD:-redispwd4readme}"]
        volumes:
            - readme-redis-database-data:/data
            - ./server/config/redis.conf:/usr/local/etc/redis/redis.conf:ro
        ports:
            - "6379:6379"
        networks:
            - readme-network
        healthcheck:
            test: [ "CMD", "redis-cli", "--pass", "${REDIS_PASSWORD:-redispwd4readme}", "ping" ]
            interval: 10s
            timeout: 5s
            retries: 5
            start_period: 10s
        restart: unless-stopped
        deploy:
            resources:
                limits:
                    cpus: "0.5"
                    memory: 512M

    postgres-database-backups:
        image: prodrigestivill/postgres-backup-local:18-alpine-d257e5d
        container_name: readme-postgres-database-backups
        restart: always
        environment:
            - POSTGRES_HOST=postgres-db
            - POSTGRES_DB=readmedb
            - POSTGRES_USER=readmepguser
            - POSTGRES_PASSWORD=pgpwd4readme
            - SCHEDULE=@every 12h00m00s
            - BACKUP_KEEP_DAYS=7
        volumes:
            - readme-postgres-database-backups:/backups
        networks:
            - readme-network
        depends_on:
            postgres-database:
                condition: service_healthy

    database-manager:
        build:
            context: .
            dockerfile: database_manager/Dockerfile
        container_name: readme-database-manager
        environment:
            - DB_HOST=database
            - DB_PORT=5432
            - DB_NAME=readmedb
            - DB_USER=readmepguser
            - DB_PASSWORD=pgpwd4readme
            - DATABASE_URL=postgresql://readmepguser:pgpwd4readme@database5432/readmedb
            - PGPASSWORD=pgpwd4readme
        stdin_open: true
        tty: true
        volumes:
            - readme-postgres-database-backups:/backups
        networks:
            - readme-network
        depends_on:
            postgres-database:
                condition: service_healthy
            redis-database:
                condition: service_healthy

    auto-migrations:
        build:
            context: .
            dockerfile: database_manager/Dockerfile
        container_name: readme-auto-migrations
        environment:
            - DB_HOST=database
            - DB_PORT=5432
            - DB_NAME=readmedb
            - DB_USER=readmepguser
            - DB_PASSWORD=pgpwd4readme
            - DATABASE_URL=postgresql://readmepguser:pgpwd4readme@database:5432/readmedb
            - PGPASSWORD=pgpwd4readme
        command: [ "migrate", "auto" ]
        networks:
            - readme-network
        depends_on:
            postgres-database:
                condition: service_healthy
        restart: "no"

    client:
        build:
            context: client
            dockerfile: Dockerfile
        container_name: readme-client
        networks:
            - readme-network
        ports:
            - "8000:8000"
        restart: unless-stopped

    server:
        build:
            context: server
            dockerfile: Dockerfile
        container_name: readme-server
        networks:
            - readme-network
        depends_on:
            postgres-database:
                condition: service_healthy
            redis-database:
                condition: service_healthy
            auto-migrations:
                condition: service_completed_successfully
        ports:
            - "8080:8080"
        restart: unless-stopped

networks:
    readme-network:
        driver: bridge

volumes:
    readme-postgres-database-data:
    readme-postgres-database-backups:
    readme-redis-database-data: